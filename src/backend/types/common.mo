module {
  /// Nanoseconds since the Unix epoch (as returned by `Time.now()`).
  public type Timestamp = Int;

  /// A player is identified by their Internet Identity principal.
  public type PlayerId = Principal;
};
